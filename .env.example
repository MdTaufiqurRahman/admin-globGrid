# Copy to .env.local and fill in.
# VITE_* values are inlined at build time, so they must be set before `pnpm build`.

# Base URL of the backend API (no trailing path) — required
VITE_API_URL=https://api.example.com
